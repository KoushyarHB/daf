services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: daf
      POSTGRES_PASSWORD: daf
      POSTGRES_DB: daf
    volumes:
      - daf_pg_data:/var/lib/postgresql/data

volumes:
  daf_pg_data:
